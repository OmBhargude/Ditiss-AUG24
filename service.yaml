apiVersion: v1
kind: Service
metadata:
  name: subenum-service
spec:
  selector:
    app: subenum
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: http  # Added name for the first port
    - protocol: TCP
      port: 8000
      name: metrics
  type: NodePort
